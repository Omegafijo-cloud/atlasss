
// El servicio de IA ha sido desactivado.
// Este archivo se mantiene como placeholder para evitar errores de importación si existieran referencias residuales.

export const getSmartResponse = async (query: string, results: any[]) => {
  return "";
};
